# Base URL for the backend API.
# In dev, leave as /api to use the Vite proxy (keeps cookies first-party).
# In production, set to your API origin.
# Deployed backend: https://authentication-system-bcll.onrender.com/api
VITE_API_URL=/api
